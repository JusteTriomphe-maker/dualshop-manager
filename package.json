{
  "name": "dualshop-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend:dev": "cd backend && node --watch src/app.js",
    "backend:start": "cd backend && node src/app.js",
    "backend:seed": "cd backend && node prisma/seed.js",
    "web:dev": "cd web && npm run dev",
    "web:build": "cd web && npm run build",
    "dev": "node dev.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
